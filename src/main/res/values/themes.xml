<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <!-- Switched parent to Theme.MaterialComponents.Light to enforce a light theme -->
    <style name="Theme.InkyTasks" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Background color. -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Text Colors -->
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <!-- Remove animations -->
        <item name="android:windowAnimationStyle">@null</item>
        <!-- Dialog Theme -->
        <item name="alertDialogTheme">@style/AlertDialogTheme</item>
        <!-- Action Bar Style -->
        <item name="actionBarStyle">@style/Widget.App.ActionBar</item>
    </style>

    <!-- Custom Action Bar Style -->
    <style name="Widget.App.ActionBar" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="background">@color/white</item>
        <item name="titleTextColor">@color/black</item>
        <item name="elevation">0dp</item>
        <!-- Style for the back arrow -->
        <item name="homeAsUpIndicator">@drawable/ic_arrow_back</item>
        <item name="android:theme">@style/Theme.App.ActionBar</item>
    </style>

    <!-- Theme for action bar icons and text -->
    <style name="Theme.App.ActionBar" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
    </style>

    <style name="AlertDialogTheme" parent="Theme.AppCompat.Light.Dialog.Alert">
        <!-- Background Color -->
        <item name="android:windowBackground">@color/white</item>
        <!-- Title Text Color -->
        <item name="android:textColorPrimary">@color/black</item>
        <!-- Message Text Color -->
        <item name="android:textColorSecondary">@color/black</item>
        <!-- Button Colors -->
        <item name="colorAccent">@color/black</item>
        <item name="buttonBarPositiveButtonStyle">@style/AlertDialog.Button</item>
        <item name="buttonBarNegativeButtonStyle">@style/AlertDialog.Button</item>
    </style>

    <style name="AlertDialog.Button" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/black</item>
    </style>
</resources>

